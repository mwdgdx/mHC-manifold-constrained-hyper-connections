# Example project env for infra scripts (no secrets)
#
# Copy to `/mnt/project.env` on the persistent volume (recommended) or to
# `infra_scripts/project.env` on the pod, then edit as needed.
#
# NOTE: Do NOT put API keys in this file. Use `wandb login` (writes `~/.netrc`)
# and any service-specific credential mechanisms.

export OPS_DEFAULT_HOST="lium"
export OPS_REMOTE_REPO="/root/work/mHC-manifold-constrained-hyper-connections"
export OPS_REMOTE_OUTPUTS_DIR="/mnt/pod_artifacts/outputs"

export LIUM_DEFAULT_VOLUME="my_volume"

# FineWeb dataset location on the mounted volume
export DATA_DIR="/mnt/data/fineweb10B"

# Optional convenience defaults for sweeps
export WANDB_GROUP="fineweb10B-sweep-$(date +%Y%m%d)"
